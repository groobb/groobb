# Dependabotのバージョン更新設定。
# 参照: https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  # GoアプリのGo module依存。
  - package-ecosystem: "gomod"
    directory: "/go"
    schedule:
      interval: "daily"
    # メジャーは破壊的変更を含むことがあり個別PRでレビューしたいため、
    # 以下のすべてのグループでpatch / minorのみを含める。
    groups:
      river:
        patterns:
          - "github.com/riverqueue/*"
        update-types:
          - "patch"
          - "minor"
      golang-x:
        patterns:
          - "golang.org/x/*"
        update-types:
          - "patch"
          - "minor"
      jackc-pgx:
        patterns:
          - "github.com/jackc/*"
        update-types:
          - "patch"
          - "minor"

  # Goアプリのアセットパイプライン用npm (pnpm) 依存。
  - package-ecosystem: "npm"
    directory: "/go"
    schedule:
      interval: "daily"
    # メジャーは破壊的変更を含むことがあり個別PRでレビューしたいため、
    # 以下のすべてのグループでpatch / minorのみを含める。
    groups:
      tailwindcss:
        patterns:
          - "tailwindcss"
          - "@tailwindcss/*"
        update-types:
          - "patch"
          - "minor"

  # CIワークフローが使うGitHub Actions。
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
