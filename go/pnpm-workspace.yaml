# pnpmは共有設定をこのファイルから読み込みます (pnpmがpackage.jsonから
# 移動)。allowBuildsはインストールスクリプトの実行を許可するパッケージを列挙します。
# esbuildと @parcel/watcher (tailwindcss --watchが使用) はpostinstallでネイティブ
# バイナリを取得しますが、pnpmは既定でこれをブロックします。
allowBuilds:
  "@parcel/watcher": true
  esbuild: true
